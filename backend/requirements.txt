# Scraper deps (existing)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
feedparser==6.0.11
APScheduler==3.10.4
pytz==2024.2

# FastAPI + async
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
pgvector>=0.2.0

# Pydantic + settings
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.9

# Async support
greenlet>=3.0.0
pydantic[email]>=2.0.0

# Cache + queue
redis[hiredis]>=5.0.0
celery[redis]>=5.3.0

# AI
anthropic>=0.40.0

# Embeddings (lokalni multilingvalni model — tekst ne napusta infrastrukturu)
# NAPOMENA: torch se instalira kao CPU-only build u Dockerfile-u (pre ovog fajla),
# pa sentence-transformers koristi vec instaliran CPU torch (bez CUDA/nvidia-* paketa).
sentence-transformers>=3.0.0

# Utils
python-dotenv>=1.0.0
